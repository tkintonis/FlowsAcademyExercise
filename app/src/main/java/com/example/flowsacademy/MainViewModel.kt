package com.example.flowsacademy

import androidx.lifecycle.ViewModel

class MainViewModel : ViewModel() {

    /** What is needed is a countdown to be exposed to the Activity via flow
    and when the user clicks the button to increment its value by 1.
     **/

    //Todo("Here write all the business logic which should be exposed to the screen")
}
